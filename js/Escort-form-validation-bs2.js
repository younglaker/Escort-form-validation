// Generated by CoffeeScript 1.6.3
var $;

$ = jQuery;

$.fn.EscortForm = function() {
  var form, selector;
  selector = "input, textarea, select";
  form = this;
  form.find(selector).parent().append("<span class='help-inline tip-red oct-tips'>aaa</span>");
  return form.submit(function() {
    var qualified, reg_email;
    qualified = true;
    reg_email = /^(\w)+(\.\w+)*@(\w)+((\.\w+)+)$/;
    form.find(selector).each(function() {
      if ($(this).attr("esRequired") === "true") {
        console.log(this.value);
        if (this.value === "") {
          $(this).parent().find(".oct-tips").html("请填写");
          return qualified = false;
        } else {
          $(this).parent().find(".oct-tips").html("888");
          return console.log("662" + qualified);
        }
      }
    });
    console.log("2" + qualified);
    if (qualified !== true) {
      return false;
    }
    console.log("55");
    form.find(selector).each(function() {
      if ($(this).attr("octmax")) {
        if (this.value > $(this).attr("octmax")) {
          $(this).parent().find(".oct-tips").html("太大了");
          return qualified = false;
        }
      }
    });
    console.log("3" + qualified);
    if (qualified !== true) {
      return false;
    }
  });
};
